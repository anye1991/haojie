# 核心依赖（AI 模型 + 数据处理）
torch>=2.2.2          # 支持 Python 3.12，AI 漏洞特征推理核心
numpy>=1.26.4         # 数据特征提取（完善数组操作兼容性）
pandas>=2.2.0         # 数据集处理与分析（补充的缺失依赖）
scikit-learn>=1.4.0   # 数据预处理（标准化、数据集拆分等，补充的缺失依赖）

# Web 渗透基础依赖
requests>=2.31.0      # HTTP 请求（Web 漏洞探测、Payload 发送）
beautifulsoup4>=4.12.3 # HTML 解析（敏感信息提取、页面结构分析）
lxml>=6.0.2           # 高效 HTML/XML 解析（配合 bs4 使用）
urllib3>=2.5.0        # HTTP 连接池（避免 requests 依赖冲突）

# 漏洞利用工具依赖
sqlalchemy>=2.0.44    # 数据库交互（SQL 注入后数据存储）
python-nmap>=0.7.1    # 端口扫描（信息收集阶段）
scapy>=2.5.1          # 网络数据包分析（基础漏洞探测）

# 辅助功能依赖
colorama>=0.4.6       # 终端彩色输出（日志可视化）
pyyaml>=6.0.1         # 配置文件解析（框架参数配置）
markdown>=3.5.2       # Markdown 渲染（PoC 报告生成）
pillow>=10.2.0        # 图片处理（漏洞截图压缩）

# 系统交互依赖（自动提权、工具调用）
paramiko>=3.4.0       # SSH 连接（Linux 提权后远程操作）
psutil>=5.9.8         # 系统资源监控（提权后权限验证）
